require('zone.js/dist/zone');
import 'reflect-metadata';

// import { enableProdMode } from '@angular/core';
//
// let nodeEnv = process.env.NODE_ENV || 'development';
//
// if (/^dev/i.test(nodeEnv)) {
//     require('zone.js/dist/long-stack-trace-zone');
// } else {
//     enableProdMode();
// }
